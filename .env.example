# Exemple de configuration pour Bloomzy (Docker)

# Chaîne de connexion pour la base SQLite persistante dans le volume Docker
SQLALCHEMY_DATABASE_URI=sqlite:////data/bloomzy.db

# Exemple pour MySQL (décommentez et adaptez si besoin)
# SQLALCHEMY_DATABASE_URI=mysql://user:password@mysql/bloomzy_db
